# KittenTTS 0.8.1 (real latest) is only published as a GitHub release wheel, never to PyPI
# (PyPI tops out at 0.1.3), so it has to be installed by direct URL rather than by name.
# Its misaki[en]>=0.9.4 dependency is now actually published (verified — this used to be an
# unresolvable pin, see git history), so this resolves cleanly, but it pulls in a much heavier
# chain than 0.1.3's pure-ONNX setup: spacy, curated-transformers, and PyTorch (~555MB) for
# misaki's text-processing/G2P pipeline. Accepted tradeoff for the newer model + clean_text
# support — actual TTS inference still runs on ONNX Runtime, not torch.
https://github.com/KittenML/KittenTTS/releases/download/0.8.1/kittentts-0.8.1-py3-none-any.whl
soundfile
numpy

# Moonshine STT (server-side fallback for browsers without native SpeechRecognition — see
# lib/voice/speech-client.ts). Used standalone via MoonshineSTTService.run_stt(), not
# Pipecat's full pipeline/transport framework, which this app has no other use for.
# Confirmed compatible alongside kittentts above despite both depending on onnxruntime.
pipecat-ai[moonshine]
